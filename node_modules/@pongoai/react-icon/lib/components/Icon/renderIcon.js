import * as React from 'react';
import { getSlots } from '@fluentui/react-utilities';
import { iconShorthandProps } from './useIcon';
export const renderIcon = (state) => {
    const { slots, slotProps } = getSlots(state, iconShorthandProps);
    return React.createElement(slots.root, Object.assign({}, slotProps.root), state.root.children);
};
//# sourceMappingURL=renderIcon.js.map