import { getNativeElementProps } from '@fluentui/react-utilities';
export const iconShorthandProps = ['root'];
export const useIcon = (props, ref) => {
    const { color, size = 'medium' } = props;
    const state = {
        color,
        size,
        components: {
            root: 'span',
        },
        root: getNativeElementProps('span', {
            ref,
            role: 'img',
            ...props,
        }),
    };
    return state;
};
//# sourceMappingURL=useIcon.js.map