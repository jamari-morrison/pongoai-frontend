import * as React from 'react';
import { getSlots } from '@fluentui/react-utilities';
import { ratingShorthandProps } from './useRating';
export const renderRating = (state) => {
    const { slots, slotProps } = getSlots(state, ratingShorthandProps);
    return (React.createElement(slots.root, Object.assign({}, slotProps.root),
        React.createElement(slots.starWrapper, Object.assign({}, slotProps.starWrapper)),
        !state.readOnly && React.createElement(slots.input, Object.assign({}, slotProps.input))));
};
//# sourceMappingURL=renderRating.js.map