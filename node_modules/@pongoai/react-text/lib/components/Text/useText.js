import { getNativeElementProps } from '@fluentui/react-utilities';
export const useText = (props, ref) => {
    const { as = 'span', size = 300, font = 'base', weight = 'regular', italic = false, underline = false, strikethrough = false, color = 'base', } = props;
    const state = {
        size: size,
        font,
        weight,
        italic,
        underline,
        strikethrough,
        color,
        components: {
            root: 'span',
        },
        root: getNativeElementProps(as, {
            ref,
            ...props,
            as,
        }),
    };
    return state;
};
//# sourceMappingURL=useText.js.map