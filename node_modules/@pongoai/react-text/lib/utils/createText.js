import * as React from 'react';
import { renderText, useText, useTextStyles } from '../Text';
import { mergeClasses } from '@fluentui/react-make-styles';
export function createText(defaultProps, useStyles, displayName) {
    const TextComponent = React.forwardRef((props, ref) => {
        const mergedProps = { ...defaultProps, ...props };
        const styles = useStyles();
        const state = useText(mergedProps, ref);
        useTextStyles(state);
        state.root.className = mergeClasses(state.root.className, styles.root, mergedProps.className);
        return renderText(state);
    });
    TextComponent.displayName = displayName;
    return TextComponent;
}
//# sourceMappingURL=createText.js.map